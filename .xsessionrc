# 2018年 09月 19日 星期三 21:34:14 CST
# for xfce4
# 因为linux的桌面环境(DE)并不会加载任何profile, bash(即不会使用此机制)
# 而是使用.xsessionrc(或其他的什么配置文件, 不用管其他的)

if [ -f '/etc/profile.d/java_path.sh' ] ; then
    . '/etc/profile.d/java_path.sh'
fi

if [ -f "$HOME/.profile" ] ; then
    . "$HOME/.profile"
fi

